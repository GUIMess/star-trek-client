{
  "name": "starfleet-intelligence-console",
  "version": "2.0.0",
  "description": "Postgres-backed Starfleet intelligence console with scan, brief, and compare workflows.",
  "main": "server.js",
  "author": "Catalin Siegling",
  "license": "ISC",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "db:setup": "node scripts/setup-db.js",
    "db:seed": "node scripts/setup-db.js --seed-only",
    "check": "node --check server.js && node --check lib/db.js && node --check lib/knowledge-service.js && node --check scripts/setup-db.js && node --check js/main.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.3"
  }
}
